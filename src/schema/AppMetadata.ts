export interface AppMetadata {
  title: string;
  description: string;
  version: string;
}

export const defaultAppMetadataValue: AppMetadata = {
  title: 'Eviate',
  description: '',
  version: '1.0.0'
}
